import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.zeuvastec.language', appName: 'Zeuvastec Language', webDir: '.', bundledWebRuntime: false };
export default config;
